{
  "name": "@anvil-vault/express",
  "version": "0.0.0",
  "keywords": [],
  "license": "ISC",
  "author": "Ada-Anvil",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsup --config build.tsup.ts",
    "check": "tsc --noEmit",
    "emit": "tsup --config emit.tsup.ts",
    "test": "vitest --run --passWithNoTests",
    "test:watch": "vitest --watch --passWithNoTests"
  },
  "dependencies": {
    "trynot": "^0.0.2"
  },
  "devDependencies": {
    "@anvil-vault/handler": "*",
    "@anvil-vault/tsconfig": "*",
    "@anvil-vault/tsup": "*",
    "@anvil-vault/utils": "*",
    "@types/express": "^5.0.3",
    "tsup": "^8.5.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.4"
  }
}
